import { PasswordHiddenDirective } from "./password-hidden.directive";

describe("PasswordHiddenDirective", () => {
  it("should create an instance", () => {
    const directive = new PasswordHiddenDirective();
    expect(directive).toBeTruthy();
  });
});
